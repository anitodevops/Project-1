# Steps for Build Phase

## Step 5: Create GitHub branch & Push Code

-	Create dev branch 

        `git branch -M dev`
 	
-	Add files to working environment

        `git add .`
 	
-	Move file from working environment to staging environment

        `git commit -m “Initial commit”`

-	Push files to git 

        `git push -u origin dev`
